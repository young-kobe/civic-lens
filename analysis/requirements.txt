fastapi>=0.120.0,<1
uvicorn>=0.30.0,<0.35
requests>=2.32.2,<3
pydantic>=2.10.0,<3
pydantic-settings>=2.5.0,<3
google-generativeai>=0.8.3
trafilatura>=2.0.0
lxml_html_clean>=0.4.0
PyYAML>=6.0.1
beautifulsoup4>=4.12
# Per-endpoint, per-IP rate limits (audit §1.6). starlette is an existing
# transitive via fastapi; slowapi just wires limits onto it.
slowapi>=0.1.9,<0.2
# Explicit starlette floor to force the transitive above the CVE range.
# CVE-2025-54121 (DoS via multipart) fixed in 0.47.2; CVE-2025-62727
# fixed in 0.49.1. fastapi >=0.120 accepts starlette 1.0 so we pin
# generously upward.
starlette>=0.49.1,<2