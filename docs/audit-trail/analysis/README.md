# Analysis audit trail

Changes to the Python analysis pipeline: ETL, engine (bot / sentiment / propaganda / narrative clustering), LLM client, aggregators, snapshot cache, scheduler.

Scope: `analysis/src/` excluding `api/`.

Out of scope: request handling / rate limits (`../api/`), ingestion (`../ingestion/`).
