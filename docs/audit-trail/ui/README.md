# UI audit trail

Changes to the React + Vite + TypeScript frontend: components, pages, services, theme, CSS.

Scope: `ui/src/`.

Out of scope: API contracts (`../api/`), backend aggregators (`../analysis/`).
