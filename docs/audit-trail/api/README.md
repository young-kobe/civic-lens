# API audit trail

Changes to the FastAPI server: routers, rate limits, admin auth, snapshot cache contract.

Scope: `analysis/src/api/`.

Out of scope: aggregators that produce the data the API serves (`../analysis/`).
