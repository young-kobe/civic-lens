# Civic Lens - Code Audit Walkthroughs

Development walkthroughs documenting all significant changes to the codebase.
Each document captures what was changed, why, and the verification results.

| # | Walkthrough | Description |
|---|-------------|-------------|
| 001 | [Initial Infrastructure](001-initial-infrastructure.md) | Project scaffolding, Go ingestor, initial database schema |
| 002 | [Frontend API Integration](002-frontend-api-integration.md) | Connecting React dashboard to FastAPI backend |
| 003 | [Dashboard UI Implementation](003-dashboard-ui-implementation.md) | Dashboard layout, card components, chart system |
| 004 | [Go Migration](004-go-migration.md) | Database migration system for Go ingestor |
| 005 | [Python Analysis Refactoring](005-python-analysis-refactoring.md) | Extracting models into dedicated modules, SOLID refactor |
| 006 | [Background Analysis Pipeline](006-background-analysis-pipeline.md) | Job runner, async analysis, cache generation |
| 007 | [LLM Integration](007-llm-integration.md) | LLM client abstraction, Gemini/Ollama support |
| 008 | [Ollama LLM Backend](008-ollama-llm-backend.md) | Local Ollama setup, Jetson Orin Nano config |
| 009 | [Robust JSON Parsing (LLM)](009-robust-json-parsing-llm.md) | Defensive JSON parsing for LLM structured output |
| 010 | [Pipeline Improvements](010-pipeline-improvements.md) | Timestamp filtering, US political content filter, coverage |
| 011 | [Dashboard Fixes](011-dashboard-fixes.md) | Fix favorability error, sentiment distribution, source mix |
| 012 | [Live Polling & Aggregators Refactor](012-live-polling-aggregators-refactor.md) | Live polling stats, modular aggregator architecture |
| 013 | [Analysis & UI Implementation](013-analysis-ui-implementation.md) | End-to-end analysis and visualization flow |
| 014 | [Dashboard Data & UX Improvements](014-dashboard-data-ux-improvements.md) | Data quality improvements, UX polish |
| 015 | [GitIgnore & Security Scan](015-gitignore-security-scan.md) | Secrets audit, gitignore hardening |
| 016 | [Agent Rules & Workflows Update](016-agent-rules-workflows-update.md) | Code style rules, DRY/SOLID enforcement |
| 017 | [Civic Lens Analysis Redesign](017-civic-lens-analysis-redesign.md) | Content hierarchy, bot detection fix, cluster improvements |
| 018 | [Analysis Refinement](018-analysis-refinement.md) | Aggregator constants extraction, code cleanup |
| 019 | [Fix X Data Ingestion Error](019-fix-x-data-ingestion-error.md) | Fix migration error for X/Twitter data |
| 020 | [X Integration & Global Heatmap](020-x-integration-global-heatmap.md) | X post extraction, models directory, heatmap |
| 021 | [LLM Reasoning & Sentiment Refactor](021-llm-reasoning-sentiment-refactor.md) | Sarcasm detection, reasoning transparency, topic visualization redesign |
| 022 | [Struct Receiver Refactor](022-struct-receiver-refactor.md) | Runner package struct-method pattern, encapsulated state |
| 023 | [Configurable Analysis Scope](023-configurable-analysis-scope.md) | Select between reddit, x, or all for processing |
| 024 | [Sentiment Caching & UI Fixes](024-sentiment-caching-ui-fixes.md) | Snapshot caching system, sentiment gauge fix |
| 025 | [Unified Text Analyzer](025-unified-text-analyzer.md) | Combined sentiment and favorability LLM pass |
| 026 | [Audit Remediation](026-audit-remediation.md) | Fix concurrency, DB bottlenecks, and dynamic migrations |
| 027 | [SQLite Optimization](027-sqlite-optimization.md) | Context graceful shutdown and SQLite write performance tuning |
| 028 | [Sentiment Polling UI Enrichment](028-sentiment-polling-ui-enrichment.md) | Polling comparison and sentiment UI enrichment |
| 029 | [Clustering Removal & LLM Hardening](029-clustering-cut-llm-hardening.md) | Cut TF-IDF clustering end-to-end; add evidence-span validation, schema validation, and prompt archival |
| 030 | [Audit Remediation — Layers 2–4](030-audit-layer-2-4-remediation.md) | UI sampling disclaimers; drop dead tables; add narrative + audit schema; promote country_code; fix geo bug |
| 031 | [UI Terminal-Density Refactor](031-ui-terminal-refactor.md) | Tokens-first Bloomberg-terminal aesthetic on white: mono numerics, eyebrow labels, sharper panels; hand-tuned all three pages |
| 032 | [Narrative Propagation Pipeline](032-narrative-propagation-pipeline.md) | Wire citations + claim-based narrative clustering; populate `narrative_citations`, `narratives`, `narrative_docs` |
| 033 | [Narrative Reader & Embedding Clustering](033-narrative-reader-and-embeddings.md) | Narratives UI tab + API + aggregator; opt-in embedding-mode clustering via Ollama (`nomic-embed-text`) so synonyms merge |
| 034 | [Review UI & ai_output_evals Writers](034-review-ui-and-eval-writers.md) | Human-in-loop review queue, golden-set marking, per-task accuracy stats; ReviewService + 3 API endpoints + Review tab |
| 035 | [Goal Narrowing & Honesty Renames](035-goal-narrowing-and-renames.md) | Narrow product goal to "sampled political discourse + narrative overlay + partial citation overlay"; rename `origin_*` → `first_seen_*` across schema/API/UI; Bot Detector rename + plain-language disclaimer; split Narratives into News/Social sections; new Home landing page (explains tool + every tab) with clickable CIVIC LENS header; political framing pass across all pages |
| 036 | [Account Tier Classification](036-account-tier-classification.md) | Hybrid curated-list + LLM classifier for X author tiers; `account_profiles` table; `data/known_accounts.yaml` seed; NarrativeAggregator exposes `first_seen_tier`; Social Media Narratives split into Elected / Affiliated / General Public / Reddit sub-cards |
| 037 | [Dynamic Account Refresh](037-dynamic-account-refresh.md) | Scrape UCSD libguide (senators + reps) to refresh `accounts.congress.{house,senate}` in the known_political_x_accounts YAML; atomic write with diff; preserves existing districts + handle casing; `run.ps1 refresh-accounts` CLI |
| 038 | [Inference Method + Frontier CHECK](038-inference-method-and-cleanup.md) | `ai_outputs.inference_method` column (llm/heuristic/deterministic) distinguishes validated LLM rows from fallback rows; dead heuristic-kwargs cleanup in analyzer.py; `pages.state` CHECK(0..3) via table rebuild |
| 039 | [Embedding Default + Confidence Filter](039-embedding-default-and-confidence-filter.md) | Default narrative clustering to embedding mode (synonyms merge); aggregators gain `aggregation_min_confidence=0.5` floor — low-confidence sentiment and bot flags no longer distort aggregates |
| 040 | [Bot Detection Rework](040-bot-detection-rework.md) | Reorient detector around LLM-driven content: stylometric signals (sentence-length variance, hedge-phrase rate, typographic purity), X account metadata (follow ratio, sustained rate, listed count), verified_type de-biasing, pre-exclusion of electeds/affiliated/gov-verified, per-author `author_bot_scores` rollup, real computations replacing aggregator stubs |
| 041 | [Cache + B1 Versioning Cleanup](041-cache-and-versioning-cleanup.md) | Cache `geo_sentiment_{window}` snapshots; simplify narrative cache to window-keyed top-100 with API-side slice (fixes variable-limit miss); populate `prompt_versions.user_prompt_template` via COALESCE upsert |
| 042 | [Propaganda Pipeline — Backend](042-propaganda-pipeline-backend.md) | LLM-driven per-doc detector for six propaganda techniques (loaded_language / name_calling / ad_hominem / appeal_to_fear / whataboutism / doubt_casting) with verbatim evidence validation; new `run_propaganda_detection` pipeline step; ai_outputs task_type='propaganda' |
| 043 | [Propaganda Surfaces](043-propaganda-surfaces.md) | PropagandaAggregator + `/api/propaganda` + cache per window; new Propaganda UI tab with technique breakdown, news-vs-social split, and evidence-span examples; NarrativeSummary gains `propaganda_score` + `bot_pushed_fraction` (040's bot rollup × 042's propaganda = "bot-pushed + heavy propaganda" narrative overlay); Review queue extended with propaganda task |
| 044 | [Ingest-Layer Audit Remediation](044-ingest-audit-remediation.md) | Applies every §1 finding from the 2026-04-19 audit: dead-code removal (ArticleRaw / RedditComment / duplicate CrawlResult / Reddit OAuth / MaxConcurrentDomain); real `ctx` threaded through crawl + article writer so shutdown is honored; `sync.WaitGroup` drain; `Frontier.EnsureRecovered` shared by all runners; `MarkDone`/`MarkFailed` consolidated; `ClaimItems` uses `UPDATE … RETURNING`; `processPage` decomposed with error-category tagging; `PushLinks` returns categorized `PushStats`; SQLite `busy_timeout` actually applied (modernc `_pragma=...` form); five new frontier tests |
| 045 | [Analysis + API Audit Remediation](045-analysis-api-audit-remediation.md) | Lands §§2, 3, 8, 9 of the 2026-04-19 audit. LLM 0-100 confidence coerce + prompt guardrail; aggregator DRY (`fetch_task_rows`, shared `bot_docs`); ETL N+1 preload; `analyzer._word_offsets` hot-path fix; unified Ollama/Gemini backoff; API `/api/v1` versioning with modular routers (health unversioned, admin/data/review split); `/health` probes DB+cache; per-endpoint pipeline-trigger rate limiting; `Literal` window types; deletes `/api/profiles` and `process_analysis_queue`; narrative clusterer audit metadata (migration 015) + anchor warm-up + `embedding_fallbacks` summary; mean (not max) claim-confidence pooling; drops unused `repost` link_type (migration 016); `BotResult.fallback_reason`; prompts moved to `llm/prompts.py` |
| 046 | [UI Audit Remediation](046-ui-audit-remediation.md) | Closes §4 of the 2026-04-19 audit. `ui/src/theme.ts` centralizes palette tokens so the 15+ hardcoded hex values across pages are gone; PublicSentiment (885→417 LoC), Review (470→222 LoC), GlobalHeatmap (376→242 LoC) broken into subcomponents / extracted styles; `services/fetchJSON` + `services/useFetch` module-level cache keyed by page/window so tab-switch is instant; retry UX unified on `refetch()` (no more `window.location.reload`); accessibility first pass (role=img / role=progressbar / aria-hidden / aria-label on charts, confidence meters, donuts, sparklines); `transformBotData` + `.grid-4` dead code removed |
| 047 | [Pre-Deploy Hardening](047-pre-deploy-hardening.md) | Five-PR remediation of the 04_20 consolidated security audit before first public cutover. PR-A Python/API (validation, CVE bump, rate limits, headers, path hardening), PR-B Go ingest (SSRF redirect guard, body-size caps), PR-C UI (URL token scrub, npm audit gate), PR-D infra-as-code (systemd hardening, Caddyfile, CF-IP UFW, fail2ban), PR-E CI/CD (`.github/workflows/deploy.yml` + restricted `deployment` user with forced-command SSH key). Deploy-day: Cloudflare Access, Authenticated Origin Pulls, Gemini key rotation |
| 048 | [Pipeline Cost Controls + Seed Refresh](048-pipeline-cost-controls.md) | Persistent monthly X API spend guard (migration 017 `x_api_budget` + `XBudgetTracker` + `x.monthly_budget_cents` config knob) enforcing a ~$25/mo ceiling; seeds.yaml refresh — X queries trimmed from 21 to 8 high-signal topics with `max_tweets_per_query` 100→10, 5 RSS feeds added (AP / Axios / WSJ Opinion / Federalist / Intercept), subreddits rebalanced (drop dead `Liberal`, add `democrats` / `AskConservatives` / `AskALiberal` / `PoliticalHumor`); total operating target ≤$48/month |
| 049 | [Launch](049-launch.md) | **Live at https://civic-lens.info as of 2026-04-21.** Cutover sequence; in-flight bug fixes during deploy (Caddy `/health` routing, Caddy 2.6 vs 2.8 `client_auth` syntax, script executable bit, migrations symlink, systemd `ProtectSystem` vs `WorkingDirectory`, starlette CVE-2025-54121/62727, Gemini schema `minimum`/`maximum` rejection, gemini-2.0-flash EOL → 2.5-flash, Reddit + RCP blocking Hetzner IP); current timer schedule; first-run data snapshot; follow-ups (Reddit OAuth, google.genai migration, UI CF Access redirect flow) |
| 050 | [Sentiment DoW + Drill-down + Mobile Fit](050-sentiment-dow-drilldown-mobile.md) | Day-of-week sentiment breakdown alongside age buckets (`DayOfWeekSentiment` model, `_get_day_of_week` + `_format_day_of_week_sentiment` in the sentiment aggregator); per-intensity drill-down samples (15 confidence-sorted docs per `strongPositive/mildPositive/neutral/mildNegative/strongNegative` bucket via `_collect_strength_sample`, surfaced in the UI as a `SampleDrawer` reusing `ClassificationSampleCard`); `SentimentOverviewHeader` compacted to a 3-stat row; Narratives row + Bot Heatmap mobile-overflow fixes; `body { overflow-x: hidden }` safety net; filter-bar `flex-wrap` on phones; tab shortLabel `Claims` → `Narratives` for consistency with desktop / Home copy |
| 051 | [Dashboard Grid + Pop-out Drill-downs](051-dashboard-grid-and-modals.md) | 12-col `.dashboard-grid` + `.col-span-{4,5,6,7,8,12}` utilities applied to Sentiment / Propaganda / Bot / Narratives pages so cards pair proportional to content density (3-stat summary at col-5 next to wider panel at col-7, heatmap at col-8 next to domain list at col-4, etc.); reusable portal-rendered `Modal` component (backdrop, Esc/click-outside, scroll-locked body, focus on close, mobile bottom-sheet variant); SentimentDistribution drill-down + TopicRow reasoning expansion both switched from inline-expand-below to pop-out modals so the page no longer shifts when a drill-down opens; GOPFavorabilityCard hero compacted into a 2-col layout (net favorability + stance distribution side-by-side); BehavioralSignalsPanel refactored to emit its four cards directly into the dashboard grid (heatmap col-8, domain list col-4, age + similarity 6+6) with extracted `SimilarityBar` helper |
| 052 | [Source Filter + Label Renames + Intensity Reframe](052-source-filter-and-label-rename.md) | Backend `source_filter` param on `SentimentAggregator.get_public_sentiment` + `PropagandaAggregator.get_propaganda_overview` + a shared `source_filter_allowed()` helper in `base.py`; `/sentiment` + `/propaganda` routes accept `?source=news/reddit/social` (non-`all` bypasses snapshot cache); UI `fetchSentiment` / `fetchPropaganda` thread `filters.sourceType` through; source-type pill enabled on every data page except Bot; dead client-side filter block removed from Propaganda. UI label renames: Net Sentiment → Overall Tone, GOP Favorability → GOP Party Stance, Net Favorability → Net Stance, Favorability Trend → Stance Trend; GOP-card `SentimentBar`s use `colorScheme="political"` so partisan stance reads blue-vs-red instead of green-vs-red. Distribution card reframed as "Tone Intensity" with Intense/Measured/Neutral as the lead stats and a dynamic "Reads as: …" one-line interpretation (heavily polarized / leans favorable / near-even etc.) |
| 053 | [Editorial Tokens + GlobalTicker](053-editorial-tokens-and-global-ticker.md) | Phase 1 of the UI Redesign Plan: foundational design-token + component pass. New `--font-lead` serif token; `.card-title` rewritten as `text-lg` serif headline (was uppercase small-caps) and `.card-subtitle` as a deck-style italic serif standfirst; new `.lead`, `.section-divider`, `.col-rule-left`/`-right` utilities reserved for later phases. New reusable `GlobalTicker` component (≈40px stat strip with mono values, optional accent color + refresh timestamp) replaces `SentimentOverviewHeader` on Public Sentiment and brings a parity overview strip to Narratives, Propaganda (retires the 3-card `MetricCard` row that duplicated the same metrics), and Bot Detector. `SentimentOverviewHeader.tsx` left on disk for Phase 11 cleanup |
| 054 | [Entity Registries](054-entity-registries.md) | Phase 2 of the UI Redesign Plan: pure content pass. Three new YAMLs under `data/` — `news_outlets.yaml` (20 outlets with domain + AllSides 2024 partisan_lean + neutral 2–3 sentence editorial blurb + owner), `verified_officials.yaml` (16 seats: President, VP, Speaker, Senate + House majority/minority leaders, RNC + DNC chairs, Secretaries of State/Treasury/Defense/Justice/DHS/Health, FBI Director — each with canonical X handle, term_start, Wikipedia bio_source, and editorial blurb), `major_subreddits.yaml` (10 subreddits with tilt + tilt_source + blurb, names stored canonical case with `r/` stripped). New `analysis/tests/test_entity_registries.py` — 22 schema-only tests validating counts, required fields, allowed enums for partisan_lean/tilt/party, handle + domain uniqueness, ISO `term_start`, URL `bio_source`, blurb length 80–1000 chars. No code consumes these yet; Phase 3 wires them into the aggregators |
| 055 | [Phase 3 Pre-Check Audit](055-phase3-pre-check-audit.md) | Phase 3a of the UI Redesign Plan: audit-only pass (no code changes). Queries `data/civic_lens.db` to characterize how registry-matchable the live data is before aggregator work begins. **News:** 89.7% of news docs match `news_outlets.yaml` after stripping the `www.` prefix the Go crawler preserves from RSS feed URLs; zero case/www collisions within a given outlet, so normalization is required once at aggregator match time but no backfill migration is. **Reddit:** 39.4% match rate (catch-all above the plan's 30% threshold, but the catch-all is dominated by intentionally non-registry subreddits like moderatepolitics + historical r/Liberal data + non-US-political subs like worldnews/news/geopolitics); no case-variant duplicates so aggregator-time case-insensitive compare is enough. **X:** 0.0% match against `verified_officials.yaml` (0 of 2,784 x_posts) because the ingest runs topic queries, not per-official timelines — deferred to Walkthrough 056 (per-official X timeline ingestion); Phase 3b aggregators will ship with an "insufficient data" state for the Verified Officials column until 056 lands. Documents follow-ups: CBS News registry add (144 docs uncovered), non-US-political subreddit trim, non-political news leakage (lendingtree.com etc.) |
| 056 | [X Official-Timeline Queries](056-x-official-timeline-queries.md) | Seeds-only fix for the Phase 3a pre-check blocker: two new `political_queries` strings in `data/seeds.yaml` use X search's `from:` operator to pull official timelines (no Go code, no new endpoints). Option-C hybrid shape: (1) pure-timeline OR across 9 high-profile officials (POTUS, VP, Speaker, House + Senate majority/minority leaders, party chairs) so their Verified Officials cards carry full-voice posts including off-topic content; (2) cabinet × topic intersection across 7 cabinet secretaries + FBI director × the 8 existing topic keywords, surfacing narrower but higher-signal posts from officials with less day-to-day media volume. `max_tweets_per_query` 10→8 and `monthly_budget_cents` 2500→3000 make room; projected spend ≈$27.50/mo against the new $30 cap. Once-daily 04:00 UTC cycle unchanged. Verification plan: first-run diagnostic + first-week budget check + registry-match rate audit; fixing handles that return 0 is a followup after the data lands. Follow-up TODO: per-official deep-pull endpoint (`GET /2/users/:id/tweets`) remains an option if option-C coverage proves insufficient |
| 057 | [Sentiment Entity Routing](057-sentiment-entity-routing.md) | Phase 3b first pass of the UI Redesign Plan. New `analysis/src/reporting/entity_registry.py` bundles YAML loading (one generic `_build_index` over 3 near-duplicate loaders), frozen `OutletEntity` / `OfficialEntity` / `SubredditEntity` dataclasses with a shared `profile_dict()` shape, module singleton, canonicalizers (`www.`, `r/`, `@` stripping), the `resolve_entity()` tier classifier and `CATCH_ALL_*` sentinels + `catch_all_profile()` factory — whole entity system in one file. `SentimentAggregator` extended with a X `tweet_id`→`author_id`→`username` LEFT JOIN and three new `byNewsOutlet` / `byOfficial` / `byGeneralPublic` rollups on `PublicSentimentResult` (each is a list of generic `EntitySentimentItem`; real entities sort before catch-alls). `TopicSentiment` gains `newsNet`/`officialsNet`/`publicNet` (+ per-tier volumes) for the Phase 5 topic-divergence panel. DRY helpers (`_build_sample_dict`, `_sample_dict_to_model`) collapse ~3 copies of identical sample-construction code; `sentiment.py` ended 155 lines shorter despite new functionality (1027 → 872) while staying monolithic to match peer aggregators. Two mid-pass refactors (initial 4-file split + separate `aggregators/entity_routing.py`) reverted after user feedback → guidance captured in `feedback_trim_boilerplate.md` memory. 60 tests pass (22 YAML schema, 22 entity registry behavior, 9 sentiment entity routing, 4 rich aggregators, 3 aggregation confidence). Narrative + propaganda extensions + UI types + fixtures deferred to walkthrough 058 |
| 058 | [Narrative + Propaganda Entity Routing + UI Types](058-narrative-propaganda-entity-routing.md) | Phase 3b second pass. `NarrativeSummary` gains `first_seen_entity_profile` / `first_seen_tier_group` / `cross_tier` via `NarrativeAggregator._registry_lookup` + `_is_cross_tier` (short-circuits on 2nd tier found). `PropagandaOverview` gains new `by_news_outlet` / `by_official` / `by_general_public` lists of `PropagandaEntityItem`; X author-handle LEFT JOIN added; `_accumulate_entity` fans rows into buckets and `_finalize_entity_items` sorts by mean_score desc with catch-alls at the end. UI `types.ts` mirrors every new field as optional (pre-Phase-3b snapshots stay valid); new `EntityProfile` + `EntitySentimentItem` + `PropagandaEntityItem` interfaces. `fixtures.ts` mocks populated via small helpers (`outletProfile` / `officialProfile` / `subredditProfile` / `catchAll` / `entityItem`) so dev mode renders the three-way frame with representative data. Tests appended to existing files: `TestNarrativeEntityRouting` in `test_account_classifier.py` (5 tests — matched news/official, unmatched X catch-all, cross-tier true/false); `TestPropagandaEntityRouting` in `test_propaganda_surfaces.py` (4 tests — outlet/official/subreddit rollups + catch-all sort). 83 tests pass; `npm run typecheck` + `npm run build` clean. Still pending: snapshot cache version bump + coverage-match logging at build time |
| 059 | [EntityProfileCard Component](059-entity-profile-card.md) | Phase 4 of the UI Redesign Plan. New `ui/src/components/common/EntityProfileCard.tsx` (~200 lines, card + detail modal inline): lean-colored left border, serif display-name + chip, italic deck blurb (120-char clamp), mono net-tone + volume + confidence stats strip, optional "Reads as:" line. Self-contained modal opens on click/Enter/Space — full blurb as `.lead` paragraph, expanded stats grid, lean-source citation, up to N classification samples via `ClassificationSampleCard`, bio link for officials. New `leanClass(profile)` helper in `theme.ts` derives one of `left/center/right/mixed/neutral` from party (officials) or lean/tilt (outlets/subreddits). CSS: hatched-border `.lean-mixed` via `repeating-linear-gradient` + `background-origin: border-box` trick; flat `.lean-*` modifiers for the other directions; color-keyed `.lean-chip-*` variants. Flat props (no nested stats object — callers pass fields they already have from `EntitySentimentItem`). `npm run typecheck` + `npm run build` clean. Deferred: per-entity 14-day sparkline (aggregators don't emit entity-level trends yet); by-topic breakdown in modal (matrix data not cheap); relocating `ClassificationSampleCard` out of `pages/publicSentiment/` when a 4th consumer appears |
| 060 | [Overall Tone Page Redesign](060-overall-tone-redesign.md) | Phase 5 of the UI Redesign Plan — "Public Sentiment" tab reframed as "Overall Tone" (tab id `sentiment` unchanged so URL hashes + cache keys don't break). New page layout: sampling disclaimer → GlobalTicker → Reads-as-today headline (serif lead, derived client-side via template that names the tier pair with biggest topic-divergence) → three-way grid of `EntityProfileCard`s (News Outlets / Verified Officials / General Public columns, up to 6 cards each, sourced from `data.byNewsOutlet` / `byOfficial` / `byGeneralPublic`) → new `TopicDivergencePanel` (replaces standalone Sentiment-by-Topic card — one row per topic, up to three positioned dots on a −100..+100 axis, sorted by range desc, click opens samples modal) → Tone Intensity (kept — walkthrough 052's Distribution card) → GOP Party Stance reframed as a "cross-cutting measure" below the three-way frame → Methodology. `PublicSentiment.tsx` 685→530 lines by gutting `DayOfWeekCard` / `SocialVsNewsCard` / `ComparisonPanel` / `TopicSentimentCard` / `LABEL_BADGE_STYLES`. Two orphaned subcomponent files deleted outright (`SentimentOverviewHeader.tsx`, `TopicRow.tsx`) instead of deferring to Phase 11. Three new CSS blocks (topic divergence axis + dots, reads-as-today card, three-way grid). `npm run typecheck` + `npm run build` clean |
| 061 | [Political Narratives Page Redesign](061-political-narratives-redesign.md) | Phase 6 of the UI Redesign Plan. Tab label "Narratives" → "Political Narratives" (id unchanged). `Narratives.tsx` rewritten to the three-way frame driven by `first_seen_tier_group` from walkthrough 058: disclaimer → GlobalTicker → Reads-as-today headline → three-way grid of compact `NarrativeCard`s (News / Official / Public columns) → `CrossTierPanel` (narratives with `cross_tier=true` — dense list with tier chips) → `AmplificationPanel` (conditional — narratives with `propaganda_score >= 0.4` OR `bot_pushed_fraction >= 0.3`). Drill-down via new `NarrativeDetailModal` (stats grid + daily-volume sparkline + source mix + first-seen entity profile). Dropped: old `NarrativeRow` + `NarrativeSection` + the elected / affiliated / general-public X-tier subdivision + the Reddit group + orphan section. Old `.narrative-row*` CSS block replaced with `.narrative-card*` + `.cross-tier-*` + `.amplification-list` + `.narrative-modal-stats`. `readsAsToday` template names the dominant tier and cross-tier count. `npm run typecheck` + `npm run build` clean |
| 062 | [Shared Primitives + Narratives Entity Grid](062-shared-primitives-and-narratives-entity-grid.md) | Cleanup + Phase 6 close-out. Three new `components/common/` primitives: `CollapsibleInfo` (`<details>` wrapper replacing four inline how-this-works copies), `TopMetricsBlock` + `TierRow` (Bloomberg-style dense header shell used by Overall Tone + Propaganda, supports single-dot or multi-dot axes for Propaganda's News-vs-Social split), and a move of `ClassificationSampleCard` from `pages/publicSentiment/` → `components/common/` so its three consumers import from the barrel. Political Narratives restructured to mirror Overall Tone: the three-way grid now renders one `EntityProfileCard` per first-seen entity (grouped via new `groupNarrativesByEntity` keyed by `${kind}:${key}`, stats = Stories/Avg tone/Supporting docs/Crossing groups via `entityStatsForNarratives`), clicking a card opens a new `NarrativeEntityModal` (EntityHeader + stats + external link + that entity's narratives as `NarrativeCard`s); clicking a `NarrativeCard` inside opens the existing `NarrativeDetailModal`. Nested-modal stacking avoided via `{activeEntity && !activeNarrative}` guard so only one portal mounts at a time. `NarrativeCard` / `NarrativeDetailModal` / `ClaimsSpreadingPanel` / `SourceBar` all kept. Mock fixtures already populate the new `first_seen_entity_profile` so dev mode renders the entity grid immediately. `npm run typecheck` + `npm run build` clean |
| 063 | [Modal Back Nav + Supporting Docs Table](063-modal-back-nav-and-supporting-docs-table.md) | Follow-on to 062. `Modal` gains `onBack` + `backLabel` props (renders a ← chevron in the title bar) so the narrative detail modal can return to the narrative-entity modal without tearing the parent down. New shared `SupportingDocsTable` primitive under `components/common/` — scrollable table (headline / source / when / tone / reasoning / external link), exported alongside a `classificationSampleToSupportingDoc` adapter so the Tone page's entity modal can reuse the same surface off its existing sample payload. Backend `NarrativeAggregator._top_supporting_docs` (LEFT JOIN `ai_outputs` sentiment, ORDER BY confidence desc, capped at 6) + `_build_source_label` / `_build_doc_url` helpers (news → http ident, reddit → synthesized `/r/{sub}/comments/{id}`, x_post → `https://x.com/{handle}/status/{ident}`) feed `NarrativeSummary.top_supporting_docs`. UI surfaces the table inside `NarrativeDetailModal` below the sparkline + source mix. Invariant + memory captured: every per-doc evidence surface must outbound-link to the original source (C1 in `docs/INVARIANTS.md`). `npm run typecheck` clean |
| 064 | [Propaganda Page Redesign](064-propaganda-page-redesign.md) | Phase 7 of the UI Redesign Plan. `Propaganda.tsx` reframed around the three-way entity grid: disclaimer → GlobalTicker → reads-as-today headline → three-way grid of `EntityProfileCard`s driven by `data.by_news_outlet` / `by_official` / `by_general_public` → `NewsVsSocialCard` (`TopMetricsBlock` with a split-dot axis showing both cohort means on one track) → technique breakdown → examples list. New `PropagandaEntityModal` opens on card click, filters the `examples[]` array to just that entity via `entityMatchesExample(item, ex)` (outlet ↔ domain, official ↔ `author_handle`, subreddit ↔ domain/reddit, catch-all ↔ unmatched kind). `ExampleRow` extracted; each row renders a `View original ↗` link backed by the new `PropagandaExample.url` field. `PropagandaAggregator._fetch_examples` LEFT JOINs `x_posts_raw` + `x_users_raw` to populate `author_handle` and synthesize `url` via the shared `_build_doc_url` helper. Fixtures expanded to 6 examples with URLs + handles. `npm run typecheck` + `npm run build` clean |
| 065 | [Bot Detector Redesign + Page-Header Consistency](065-bot-detector-redesign.md) | Phase 8 of the UI Redesign Plan + a cross-page consistency pass. Bot page now leads with a reads-as-today headline named off automation-rate band / top cluster / top amplification narrative. Old yellow "how to read" warning moved into a `CollapsibleInfo` at the bottom (matches the other data pages). Eyebrow format standardized across all four data pages to `As of {formatTimeWindow(range)}` ("As of last 7 days") via new `ui/src/services/timeWindow.ts` (replaces three local `WINDOW_LABEL` copies). Every data page now renders GlobalTicker → reads-as-today → content in the same order; Tone page previously missing the first two, `buildSentimentTickerItems` + `readsAsToday` added there. Propaganda examples gain `url` + `author_handle` + `View original ↗` links backed by a new invariant (C1 in `docs/INVARIANTS.md`): every per-doc evidence surface must outbound-link to the original. `"Amplification by tier"` bullet left deferred — waiting on a bot-side entity rollup aggregator (landed in walkthrough 066) |
| 066 | [Movers Ticker + Bot Entity Rollups + Geo Removal](066-movers-ticker-and-geo-removal.md) | Phase 12 + Phase 8 close-out + dead-code removal. New `MoversAggregator` (`analysis/src/reporting/aggregators/movers.py`) computes window-over-window deltas in per-entity political tone and overall GOP favorability via two SQL passes (current window vs. equivalent preceding window from new `base.get_previous_window_range`); catch-alls excluded, per-entity volume floor 10, top 10 by `|delta_pts|`. `/movers` endpoint live-computed at 20/min. New `MoversTicker` UI primitive renders a 60s CSS marquee (pauses on hover, disabled under `prefers-reduced-motion`) below the GlobalTicker on Overall Tone + Political Narratives — user-framed as "biggest movers in political tone and GOP favorability". Phase 8's deferred "Amplification by tier" bullet filled in: new `BotEntityItem` + `BotOverview.by_news_outlet/by_official/by_general_public` + `bot.py::_fetch_entity_rollups`; `BotActivityProfiler.tsx` renders a three-way grid with per-entity `bot_rate_pct` thresholded red >10% / amber >3% / neutral. Decommissioned: the unused geo-sentiment stack (GlobalHeatmap page, `/geo-sentiment` endpoint, `GeoAggregator`, `geography` filter, heatmap theme tokens + CSS, `COUNTRY_NAMES` dict, related tests, stale snapshot cache). `npm run typecheck` clean, Python imports verified |
